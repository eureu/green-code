import React from 'react';
import { createPortal } from 'react-dom';

const ModalAddAnimal = () => {


    return (createPortal(
        <div className={StylePropertyMap.from}>

        </div>, document.body
    ))
}